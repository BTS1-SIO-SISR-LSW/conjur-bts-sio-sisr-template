name: YAML reminder

on:
  push:
  pull_request:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Rappel pédagogique : vérifiez l'indentation YAML des policies Conjur."
